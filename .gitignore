# Workspace archive upload
VORTEXBM.zip

# OS and editor noise
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*.tmp
*.log

# Node/build artifacts
node_modules/
dist/
build/
coverage/
.vercel
.firebase/

# Supabase CLI
supabase/.temp/

# Stray files from mistyped shell redirects (e.g. "iongh auth status")
*vercel_env.json
gh auth status
*gh auth status*

# Environment and secrets — keep last so the negations win
.env*
!.env.example
!.env.sample
